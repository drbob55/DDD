{
  "name": "dental-platform",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "db:generate": "pnpm --filter @dental/database db:generate",
    "db:push": "pnpm --filter @dental/database db:push",
    "db:studio": "pnpm --filter @dental/database db:studio"
  }
}